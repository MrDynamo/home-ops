---
apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: postgres-${APP}
  namespace: postgres
spec:
  name: ${APP}
  owner: ${OWNER}
  cluster:
    name: private-psql-v17
  extensions: ${EXTENSIONS:=${emptyArray}}
